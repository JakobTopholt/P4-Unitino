// Blinking Light
pin builtin = 13;prog {
	setpin(             builtin, 
	output)
	;
}
loop            {
    writepin        (builtin, high);
delay(1000);
            	writepin(builtin, low);
	delay(          1000);
}
